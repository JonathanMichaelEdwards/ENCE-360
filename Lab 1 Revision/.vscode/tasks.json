{
    "tasks": [
        {
            "type": "shell",
            "label": "clang-4.0 build active file",
            "command": "/usr/bin/clang-4.0",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}